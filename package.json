{
  "name": "@rule_generate/rule",
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "start": "bun run clean:output && bun run index.ts",
    "download": "bun run clean:storage && bun run ./download/index.ts",
    "clean:output": "rimraf ./dist",
    "clean:storage": "rimraf ./storage",
    "test:parse": "bun run ./parse/index.ts",
    "lint:fix": "eslint --fix ."
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "rimraf": "^5.0.5",
    "tldts": "^6.1.18"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^2.16.0",
    "@types/bun": "latest",
    "eslint": "^9.1.1"
  }
}
