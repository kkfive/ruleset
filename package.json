{
  "name": "@rule_generate/rule",
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "clean:output": "rimraf ./dist",
    "start": "pnpm run clean:output && bun run index.ts",
    "clean:storage": "rimraf ./storage",
    "download": "bun run ./download/index.ts",
    "test:parse": "bun run ./parse/index.ts",
    "lint:fix": "eslint --fix ."
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "rimraf": "^5.0.5",
    "tldts": "^6.1.18"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^2.16.0",
    "@types/bun": "latest",
    "eslint": "^9.1.1"
  }
}
